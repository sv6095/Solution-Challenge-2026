# secrets/
# Place your GCP service-account JSON key here as gcp-sa.json.
# This file is listed in .gitignore — the key is NEVER committed to git.
#
# Example:
#   cp /path/to/your-sa-key.json secrets/gcp-sa.json
